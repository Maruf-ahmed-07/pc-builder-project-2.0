########################################
# Example Environment File (server/.env.example)
# Copy to server/.env and fill real values locally only.
########################################

NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb+srv://DEV_USER:DEV_PASSWORD@cluster0.mongodb.net/pcbuilder?retryWrites=true&w=majority
JWT_SECRET=dev_jwt_secret_change_me_64_chars_min
SESSION_SECRET=dev_session_secret_change_me_64_chars_min
JWT_EXPIRE=30d

CHAT_MODE=rest
CORS_ORIGINS=http://localhost:3000
COOKIE_SECURE=false
COOKIE_SAMESITE=none
LOG_REQUESTS=true

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_app_password

MAX_FILE_SIZE=5000000
FILE_UPLOAD_PATH=./public/uploads

RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

GEMINI_API_KEY=

# Production overrides (set ONLY in hosting provider):
# NODE_ENV=production
# MONGODB_URI=<prod cluster>
# JWT_SECRET=<strong 64+ chars>
# SESSION_SECRET=<strong 64+ chars>
# CORS_ORIGINS=https://your-frontend-domain
# COOKIE_SECURE=true
# LOG_REQUESTS=false
# EMAIL_* real app credentials
# GEMINI_API_KEY=<prod key or leave empty if unused>NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb+srv://marufjr0:YOUR_PASSWORD_HERE@cluster0.3nkrbal.mongodb.net/pcbuilder?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure
JWT_EXPIRE=30d

# Email Configuration (for contact forms and notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# File Upload Configuration
MAX_FILE_SIZE=5000000
FILE_UPLOAD_PATH=./public/uploads

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100
